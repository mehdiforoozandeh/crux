# SortLab — t55 write a checker that the
import time

def main():
    t0 = time.perf_counter()
    print('t55 write a checker that the')
    print(round((time.perf_counter() - t0) * 1000, 3), 'ms')

if __name__ == '__main__':
    main()
