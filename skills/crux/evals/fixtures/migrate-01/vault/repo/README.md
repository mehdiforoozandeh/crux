# tokeniser-study

We wanted to know whether a bigger vocabulary helps the small model, and whether it costs
anything at inference. Two things were run; a third was written down and never started.

- `notes/vocab.md`  — the vocabulary sweep, run
- `notes/latency.md` — the latency check, run
- `notes/morphology.md` — proposed, never run
