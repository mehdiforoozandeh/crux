# Audit fixture — taskhub

_Generated by `crux`. Do not edit by hand — edit the task files under `tasks/`, which nothing regenerates._

Everything this project has to **do**. A task is an action; a claim about the world that could be true or false is a hypothesis and belongs in the tree. `blocked` is computed from the dependency graph, never stored.

## Frontier — ready to work now

- `t1` [implementation] wire the pooled readout into the eval harness — refs [[h2_the_pooled_readout_beats_the_linear_one\|h2]]
- `t2` [data-acquisition] collect the held-out split — refs [[h404]]

## By category

### data-acquisition

- `t2` [data-acquisition] collect the held-out split — refs [[h404]] — *open*

### implementation

- `t1` [implementation] wire the pooled readout into the eval harness — refs [[h2_the_pooled_readout_beats_the_linear_one\|h2]] — *open*
